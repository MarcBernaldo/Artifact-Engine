id: web_access
name: "Web access timeline"
description: "All Apache/nginx access logs (current + rotated + per-vhost) parsed into one clean request timeline"
os: linux
category: web
provides: [web_access_csv]
handler: "artifact_engine.handlers.lin_web_access:run"
