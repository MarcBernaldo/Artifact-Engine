id: auditd_config
name: "Auditd config & tampering"
description: "Auditd coverage (watch/syscall rules, task,never, -e state), weakened auditd.conf settings, and rules removed since the previous loaded ruleset (anti-forensics)"
os: linux
category: systeminfo
provides: [auditd_config_csv]
handler: "artifact_engine.handlers.lin_auditd_config:run"
